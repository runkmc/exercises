noun = String.strip IO.gets("Enter a noun\n")
verb = String.strip IO.gets("Enter a verb\n")
adj = String.strip IO.gets("Enter a adjective\n")
adv = String.strip IO.gets("Enter a adverb\n")

IO.puts("Do you #{verb} your #{adj} #{noun} #{adv}? That's hilarious!")
